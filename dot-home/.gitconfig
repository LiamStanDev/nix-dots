[user]
    name = Liam
    username = LiamStan
    email = "geffc1454@gmail.com"

[init]
	defaultBranch = master

[core]
    pager = delta
    editor = nvim

[pull]
	rebase = true

[merge]
    ff = no

[delta]
    navigate = true    # use n and N to move between diff sections
    side-by-side = false
    line-number = true

[mergetool]
	keepBackup = false # not generate .oring file



# [diff]
#     algorithm = patience
#     compactionHeuristic = true
#     tool = nvimdiff

# [difftool "nvimdiff"]
#     cmd = nvim -d $LOCAL $REMOTE -c 'wincmd w' -c 'wincmd L'

# [merge]
#     tool = nvimdiff4
#     prompt = false

# [mergetool "nvimdiff4"]
#     cmd = nvim -d $LOCAL $BASE $REMOTE $MERGED -c 'wincmd W' -c 'wincmd J'
